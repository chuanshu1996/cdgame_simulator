name = "cdgame-api"
main = "index.js"
compatibility_date = "2024-09-23"

# D1 数据库绑定（database_id 在执行 `wrangler d1 create cdgame` 后填入）
[[d1_databases]]
binding = "DB"
database_name = "cdgame"
database_id = "4f892d1e-f902-4a19-b229-365bae6d3c48"

# 跨域：设为你的 GitHub Pages 地址；本地调试可留 "*"
[vars]
CORS_ORIGIN = "https://chuanshu1996.github.io"
